@model IEnumerable<DigitalArt.Models.Opinione>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreOpinion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescripcionOpinion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaRegistro)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreOpinion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescripcionOpinion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaRegistro)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdOpinion">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdOpinion">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdOpinion">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
